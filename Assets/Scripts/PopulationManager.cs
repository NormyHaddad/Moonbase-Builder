using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PopulationManager : MonoBehaviour
{
    public float growthAmount; // The max amount of people that can arrive at once
    public float cooldownPeriod; // The cycle duration of usage of food/water new arrivals
    public float foodUseRate; // The amount of food and water used each cycle

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
